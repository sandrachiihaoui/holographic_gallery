import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'holographic-gallery',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
